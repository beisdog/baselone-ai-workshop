meta {
  name: extract-tables
  type: http
  seq: 4
}

post {
  url: http://localhost:8000/extract?extract_images=false&extract_tables=true
  body: multipartForm
  auth: inherit
}

params:query {
  extract_images: false
  extract_tables: true
  ~ocr_extracted_images: true
  ~image_ocr_min_width: 100
  ~image_ocr_min_height: 100
  ~image_ocr_max_width: 3000
  ~image_ocr_max_height: 3000
}

headers {
  : 
}

body:multipart-form {
  data: @file()
}

settings {
  encodeUrl: true
}
